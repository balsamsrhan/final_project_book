import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class FavirteScreens extends StatefulWidget {
  const FavirteScreens({ Key? key }) : super(key: key);

  @override
  State<FavirteScreens> createState() => _ChoessLoginState();
}

class _ChoessLoginState extends State<FavirteScreens> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("تسجيل eeee"),
      ),
    );
  }
}